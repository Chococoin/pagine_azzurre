// Legacy URL alias — the Header dropdown still points to /orderlist/seller
// ("Attività"). Re-render the main /orderlist page which already filters
// by seller for non-admin users.
import OrderListPage from '../page';

export default OrderListPage;
